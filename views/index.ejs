<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Google OAuth with Express.js</title>
</head>
<body>
  <h1>Google OAuth with Express.js</h1>

  <% if (user) { %>
    <h2>Welcome, <%= user.name %></h2>
    <img src="<%= user.photoUrl%>" alt="User Image" width="100">
    <p>Email: <%= user.email %></p>
    <a href="/logout">Logout</a>
  <% } else { %>
    <a href="/auth/google">Login with Google</a>
  <% } %>

</body>
</html>